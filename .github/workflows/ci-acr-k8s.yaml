---
name: 01 Azure ACR-K8s Dev Continuous Integration
on:
  pull_request:
    branches:
    - new-workloads
  workflow_dispatch:
    inputs:
      terraform_cli_version:
        description: 'Terraform CLI Version'
        required: true
        default: 'latest'
        type: string

jobs:
  validate_and_plan:
    uses: jluris-enterprise/alz-mgmt/.github/workflows/ci-template-acr-k8s.yaml@new-workloads
    name: 'CI'
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    with:
      root_module_folder_relative_path: 'workloads/k8s/dev'
      terraform_cli_version: ${{ inputs.terraform_cli_version }}
